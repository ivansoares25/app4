import React, { Component } from 'react';
import { View } from 'react-native';
import ListaItens from './source/components/ListaItens';

export default class App extends Component {
  render() {
    return (
        <ListaItens />
    );
  }
}
